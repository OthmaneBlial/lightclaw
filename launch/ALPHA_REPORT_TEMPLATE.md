# Private Alpha Report Template

Submit this only with synthetic or explicitly publishable data. Do not attach raw receipts,
prompts, repository names/content, local paths, Telegram identities, tokens, provider
request IDs, or screenshots containing them.

## Environment

- LightClaw commit or release:
- OS and version:
- Python version:
- Install method (`pipx`, `uv tool`, isolated virtual environment, container):
- Capability profile used (`observe`, `workspace-write`, or separately confirmed trusted):

## Bounded outcomes

- Install completed: yes / no
- Deterministic demo completed: yes / no
- Approximate time to deterministic success:
- `lightclaw doctor` overall status (no raw output required):
- Real Telegram task attempted with disposable data: yes / no
- Approximate time to first real Telegram result, if attempted:
- Expected artifact/receipt produced: yes / no / not attempted
- Undo or recovery path understood and tested on disposable data: yes / no / not attempted

## Actionable evidence

- Smallest sanitized reproduction of any failure:
- Expected behavior:
- Actual behavior:
- Documentation step that was unclear:
- One thing that should remain intentionally out of scope:

## Consent

- [ ] The maintainer may include these yes/no/timing values in an anonymous aggregate.
- [ ] The maintainer may quote the following sentence publicly (optional exact sentence):

The aggregate must publish total reports, successes, failures, missing values, tested
versions, and collection dates. It must not infer answers for fields a tester omitted.

