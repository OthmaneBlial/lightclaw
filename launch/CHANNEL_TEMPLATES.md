# Channel Templates

Use the single [canonical headline](HEADLINE.md). Replace every bracketed field with current,
verified evidence; remove the post if required evidence does not exist.

## Concise technical article

Start with the phone-request-to-reviewed-patch workflow. Explain the approval boundary,
minimal delegated environment, owned task workspace, private receipt, and explicit Run Card
export. Embed the short deterministic clip and trust-boundary diagram. Link the five-minute
quickstart, three recipes, raw benchmark JSON/CSV, release, threat model, and limitations.
End with what failed in alpha and what remains intentionally out of scope.

## Show HN

**Title:** Show HN: LightClaw – [canonical headline, sentence case]

I built LightClaw because [one observed problem from real alpha]. The smallest token-free
path is `[exact quickstart command]`; it produces [actual artifacts] in [measured time and
environment]. The security boundary is [one factual sentence]. Current limitations:
[three current limits]. Source/release: [URL]. I would especially value reproducible reports
about [one bounded question].

## Python/self-hosted community

Lead with the relevant implementation detail, not a generic announcement: packaging,
durable SQLite jobs, Telegram authorization, provider contracts, or receipt redaction.
Include a minimal reproduction and raw evidence. State the maintainer relationship and ask
one answerable technical question. Do not cross-post where project promotion is disallowed.

## Telegram demo post

Label the clip **deterministic fixture** or **real sanitized Telegram verification**. State
whether provider calls occurred, the capability profile, what was reviewed, actual checks,
and where the receipt stayed. Never show a token, user identity, repository name, local
path, raw prompt, or private receipt.

