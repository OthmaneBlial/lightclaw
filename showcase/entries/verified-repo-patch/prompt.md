# Public synthetic prompt

Add a health check to a tiny synthetic Python service, test it, and return only verified
work. Show the bounded plan before execution and produce a reviewable patch rather than
publishing anything.

