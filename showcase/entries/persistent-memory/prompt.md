# Public synthetic prompt

Remember this synthetic fixture fact across a restart: the project launch code is
`Orchid-47`. Then answer: “What is the project launch code?”

The code is deliberately fictional and exists only in the committed deterministic demo.

