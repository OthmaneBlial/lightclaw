# Result

The fixture stores the synthetic fact, closes the database, opens a new memory-store
instance, and retrieves `Orchid-47` for the matching namespace. The receipt acceptance
check passes and `artifact/recall.json` records the bounded query/result evidence.

This proves deterministic lexical recall and persistence for this fixture. It does not
prove semantic understanding, hosted-model quality, or recall across unrelated namespaces.

