# Redacted setup

- LightClaw: current checkout installed in an isolated Python environment.
- Scenario: built-in `memory` fixture.
- Storage: temporary local SQLite database created inside the requested output directory.
- Provider, Telegram account, network, credentials, user repository: none.

No real conversation, identity, path, or repository was used.

